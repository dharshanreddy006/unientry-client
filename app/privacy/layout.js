export const metadata = {
  title: "Privacy Policy — UniEntry GLOBAL",
  description: "Privacy policy detailing how we protect and manage user data on the UniEntry GLOBAL platform.",
};

export default function PrivacyLayout({ children }) {
  return children;
}
