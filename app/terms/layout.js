export const metadata = {
  title: "Terms & Conditions — UniEntry GLOBAL",
  description: "Terms and conditions governing the use of the UniEntry GLOBAL student ecosystem platform.",
};

export default function TermsLayout({ children }) {
  return children;
}
